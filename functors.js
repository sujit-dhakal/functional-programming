// a functor is a type that can be mapped over
// an array can be mapped over i.e we can apply function to each element in the array

numbers = [1,2,3,4]

squareOfNumbers = numbers.map((num)=>num**2)

console.log(squareofNumbers)