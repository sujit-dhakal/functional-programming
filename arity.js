// it is the number of arguments that a function takes
// arity of one means it have only one argument

const number = (x)=>(console.log(x))

const addTwoNumbers = (x,y) => {
    return x+y
}

number(5)
output = addTwoNumbers(2,3)
console.log(output)